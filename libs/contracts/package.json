{
  "name": "@zk-kv/contracts",
  "version": "0.0.1",
  "type": "module",
  "description": "",
  "author": "",
  "license": "Apache-2.0",
  "keywords": [
    "mina-zkapp",
    "mina-zk-app",
    "mina-dapp",
    "zkapp"
  ],
  "scripts": {
    "build": "nx run contracts:build",
    "buildw": "nx run contracts:build --watch",
    "coverage": "nx run contracts:test --coverage",
    "format": "nx format:write",
    "lint": "nx run contracts:lint --fix",
    "test": "nx run contracts:test",
    "testw": "nx run contracts:test --watch",
    "typecheck": "nx run contracts:typecheck"
  }
}
